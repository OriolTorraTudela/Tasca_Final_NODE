# Servidor
PORT=3000
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://localhost:27017/projecte-t9

# JWT
JWT_ACCESS_SECRET=canvia_aquest_secret_access_molt_llarg_i_segur
JWT_REFRESH_SECRET=canvia_aquest_secret_refresh_molt_llarg_i_segur
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d

# Redis (rate limiting)
REDIS_URL=redis://localhost:6379

# Email (nodemailer - per a recuperació de contrasenya)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=el_teu_email@gmail.com
EMAIL_PASS=la_teva_contrasenya_app
EMAIL_FROM=noreply@projecte-t9.com

# Frontend URL (per als links de reset)
FRONTEND_URL=http://localhost:3000
